#include <iostream>
#include <vector>

template<class T>
using vector = std::vector<T>;

class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        
    }
};

int main() {

    return 0;
}